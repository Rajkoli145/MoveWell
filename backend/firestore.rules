rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The mobile/web app uses the authenticated backend API. Client SDK access
    // is deliberately denied so profile validation cannot be bypassed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
